spring:
  application:
    name: user-service

  config:
    import: optional:configserver:${CONFIG_SERVER_URL:http://localhost:8888}, optional:file:.env[.properties]

  cloud:
    config:
      fail-fast: true

eureka:
  instance:
    hostname: localhost
