server:
  port: 8888

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Ravindranath-123/SkillSync_config-server
          clone-on-start: true
          default-label: main
          search-paths: configs