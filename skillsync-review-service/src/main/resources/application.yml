spring:
  application:
    name: review-service

  config:
    import: optional:configserver:${CONFIG_SERVER_URL:http://localhost:8888}, optional:file:.env[.properties]

  cloud:
    config:
      fail-fast: true
logging:
  level:
    com.skillsync.review.client: DEBUG
    feign: DEBUG

eureka:
  instance:
    hostname: localhost
